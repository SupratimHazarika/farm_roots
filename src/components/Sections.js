import React from 'react'
import SectionHeading from './SectionHeading'

const Sections = ({ sectionName , sectionImg, plantName, rating, price }) => {
  return (
    <div>Sections</div>
  )
}

export default Sections